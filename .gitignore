# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# generated at build/dev time by scripts/zip-form-builder.mjs
/public/form-builder.zip

# generated at build/dev time by scripts/build-registry.mjs + `shadcn build`
# (Phase 2 registry — see docs/adr/0003-packaging-split-distribution.md; the
# CLI at cli/ still uses the built fb-theme.json for its cssVars-injection
# step, everything else is a direct copy, not a `shadcn add`)
/registry/registry.json
/public/r/

# built output of the form-builder/ package (tsup) — publishable artifact,
# not source
/form-builder/dist

# production
/build

# misc
.DS_Store
*.pem

# browser automation artifacts
.playwright-mcp/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
