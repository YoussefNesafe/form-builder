{
  "name": "@yourscope/form-builder",
  "//name": "PLACEHOLDER — the owner renames this (and drops the private:true below) before the first real `npm publish`. See docs/plans/2026-07-18-packaging-publish-runbook.md's CLI-publish section.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One-command installer: copies the Form Builder engine + fields + shadcn primitives + theme into a consumer project as a single self-contained, editable form-builder/ folder. See docs/adr/0003-packaging-split-distribution.md.",
  "bin": {
    "form-builder": "bin/form-builder.mjs"
  },
  "files": [
    "bin",
    "vendor",
    "src/cli.mjs",
    "src/detect.mjs",
    "src/install.mjs",
    "src/plan.mjs",
    "src/rewrite.mjs",
    "src/source.mjs",
    "src/theme.mjs"
  ],
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "prepack": "node scripts/vendor.mjs && node scripts/assert-self-sufficient.mjs",
    "prepublishOnly": "node scripts/vendor.mjs && node scripts/assert-self-sufficient.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
